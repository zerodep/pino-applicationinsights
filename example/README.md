# Example app

## Configuration

You need a `.env` file with the application insights connection string

```sh
applicationinsights.connectionstring=<APPLICATIONINSIGHTS_CONNECTION_STRING>
```

or you can put it in `example/logger.js` instead of the configured one.

## Start

```sh
npm i
node example/app.js
```
